# גיבוי — העיצוב המקורי

הקבצים כאן הם האתר **כפי שהיה רגע לפני** עיצוב "מים ונייר" (9 באוגוסט 2026).

| קובץ | מה זה |
|---|---|
| `index-original-2026-08-09.html` | האפליקציה המלאה בעיצוב הישן. אפשר לפתוח אותו ישירות בדפדפן, הוא עובד לבד. |
| `README-original-2026-08-09.md` | ה-README כפי שהיה אז. |

הנתונים שלך **לא** נמצאים כאן ולא מושפעים מכלום — הם שמורים ב-localStorage של הדפדפן
ובגיבוי/שחזור שבתוך האפליקציה, לא בקוד.

## איך מחזירים

**רק להסתכל, בלי לשנות כלום:** פותחים את `index-original-2026-08-09.html` בדפדפן.

**להחזיר את כל העיצוב הישן:**

```
git checkout cea7c8c -- index.html
```

**להחזיר רק פריט אחד** (צבע, מרווח, כפתור מסוים): אפשר להעתיק את הכלל הרלוונטי
מהקובץ הישן — או פשוט לבקש, ולציין מה להחזיר.

## נקודת השחזור ב-git

הקומיט `cea7c8c` הוא האחרון לפני העיצוב, והוא נשאר בהיסטוריה לתמיד:

```
git show cea7c8c
git diff cea7c8c -- index.html
```

כלומר יש שתי דרכים בלתי תלויות לחזור אחורה — הקובץ בתיקייה הזאת, וההיסטוריה
עצמה. גם אם התיקייה תימחק, `cea7c8c` עדיין שם.
